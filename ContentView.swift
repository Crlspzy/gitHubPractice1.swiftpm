import SwiftUI

struct ContentView: View {
    var body: some View {
     Text("Evan")
        Text("Olivia")
        
        
        Button {
            name = ""
        } label: {
            Text("Clear")
        }

    }
}
